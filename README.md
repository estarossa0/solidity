# solidity

A learning monorepo of independent Solidity contract projects, each in its own folder.

- **[Pool](src/pool/Pool.sol)** — a constant-product (`x * y = k`) AMM with a 0.3% swap fee and ERC20-style liquidity shares.
